# 자비스 개발 순서

## 결론

전체 API 명세서를 먼저 작성하지 않는다. 핵심 데이터 구조를 정한 뒤 `옷 직접 등록 → DB 저장 → 옷장에 표시` 한 줄을 실제로 완성한다. 이 과정에서 필요한 서버 기능만 작은 API 계약으로 정의한다.

## 권장 스택

- 앱: Expo + React Native + TypeScript
- 화면 이동: Expo Router
- 데이터베이스·로그인·이미지: Supabase
- 비밀 키가 필요한 작업: Supabase Edge Functions
- 로컬 백엔드 연습: Docker Desktop + Supabase CLI
- AI: 서버 함수에서만 호출하고 앱에 API 키를 넣지 않는다

## 1단계 — 데이터 기반 만들기

먼저 아래 테이블만 만든다.

- `profiles`: 사용자 기본 정보
- `clothing_items`: 상품명, 카테고리, 색상, 이미지, 출처
- `imports`: Gmail·캡처 가져오기 작업 상태
- `outfits`: 추천한 상의·하의·아우터·신발 조합
- `wear_logs`: 실제 착용 날짜와 코디

첫 번째 완료 기준:

1. 로그인한다.
2. 옷을 직접 등록한다.
3. Supabase에 저장된다.
4. 앱을 다시 켜도 옷장에 남아 있다.

## 2단계 — Gmail 가져오기

Google 로그인과 Gmail 권한은 별도 흐름으로 구현한다. 앱이 Gmail을 직접 읽지 않고 서버 함수가 처리한다.

필요한 서버 기능만 계약을 정한다.

- `POST /gmail/connect`: Gmail 연결 시작
- `POST /gmail/sync`: 주문메일 검색 및 동기화
- `GET /imports/:id`: 가져오기 진행 상태 확인

처음에는 무신사 주문메일 한 종류만 지원하고, 취소·중복 처리를 확인한 뒤 쇼핑몰을 늘린다.

## 3단계 — AI 넣기

AI는 아래 순서로 도입한다.

1. 메일·캡처에서 상품명, 옵션, 색상 추출
2. 상의·하의·아우터·신발 카테고리 분류
3. 추천 후보 중 상황에 맞는 조합 순위 결정
4. 사용자에게 보여줄 추천 이유 생성

추천 전체를 AI에게 바로 맡기지 않는다. 날씨·카테고리·최근 착용 여부로 후보를 코드에서 먼저 만들고, AI는 후보의 순위와 설명을 담당하는 혼합 방식이 안정적이다.

AI 응답은 자유로운 문장이 아닌 고정 JSON 구조로 받고, 서버에서 검증한 뒤 저장한다. 실패 시 재시도와 규칙 기반 결과를 준비하고, 입력·출력·사용자 선택을 기록해 추천 품질을 평가한다.

## 바로 진행할 작업

1. ✅ `profiles`, `clothing_items`를 포함한 초기 DB와 RLS 설계
2. ✅ Expo 앱에 Supabase 클라이언트와 세션 저장 방식 연결
3. ✅ 직접 등록 저장·옷장 조회 데이터 계층 구현
4. ✅ 이메일 회원가입·로그인·로그아웃 화면 구현
5. Supabase 프로젝트 생성 후 마이그레이션 적용
6. 실제 계정으로 저장·재조회 확인
7. 이미지 업로드와 상품 수정·삭제 추가

이 여섯 단계가 끝난 뒤 Gmail API와 AI 추출을 시작한다.
